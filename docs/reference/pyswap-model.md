# Model and Result

Each SWAP model can be built, validated and run using the `Model` class.
The result is captured in a `Result` instance, which can be using to retrieve output logs and data.

## Model

<!-- prettier-ignore-start -->
::: pyswap.model.model
    options:
        members:
            - Model
            - run_parallel
            - ModelBuilder
            - ModelRunner
            - ResultReader
<!-- prettier-ignore-end -->

## Result

::: pyswap.model.result
